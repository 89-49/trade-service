spring:
  application:
    name: trade-service
  profiles:
    active: kafka, topics
  kafka:
    listener:
      ack-mode: record

  config:
    import: optional:configserver:http://34.50.50.170:13100
